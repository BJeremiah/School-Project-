// Single source of truth for the backend URL.
// Now pointing at the live Render deployment — no more updating this
// for local IP/hotspot changes.
export const API_URL = 'https://tesa-backend.onrender.com';
